{
  "name": "nectar-js-tests",
  "private": true,
  "description": "Headless unit tests for cross-platform article-rendering JS (Shared/Article Rendering/*.js) that doesn't otherwise have a way to be exercised outside a real WKWebView. Scoped to this directory only -- not part of the Xcode build, not a dependency of the app target. See annotations/README.md.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test annotations/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
